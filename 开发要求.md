# BetterVillagers 插件开发规格说明
> 注:此开发由Claude Fable5进行基础编写 由GLM5.2补充 最终由GPT5.6Luna审核

## 技术栈
- **开发语言**: Java 25
- **构建工具**: Maven
- **目标平台**: Minecraft Server (Spigot/Paper/Folia)

### 0.1 平台与 Folia 支持
- **完全支持 Folia**：Folia 采用区域化多线程模型（Regionized Multithreading），不存在单一主线程。插件必须做到：
  - **禁止**使用 `Bukkit.getScheduler().runTask()` / `runTaskLater()` / `runTaskAsynchronously()` 等传统调度 API（在 Folia 上会抛出 `UnsupportedOperationException`）。
  - **禁止**在非所属线程直接读写实体/方块状态，必须通过区域调度器（`RegionScheduler`）或实体调度器（`EntityScheduler`，如 `entity.getScheduler()`）将操作调度回对应区域线程执行。
  - **禁止**假设任何 Bukkit API 调用是线程安全的；所有与游戏世界（实体、方块、位置）的交互必须显式指定执行线程。
  - 通过统一的调度抽象层（推荐使用 **FoliaLib**）在 Paper 与 Folia 之间透明切换，确保同一份代码在两端均可运行。
- **Paper 兼容**：在 Paper/Spigot 上回退为传统的 `BukkitScheduler` / `JavaPlugin` 主线程模型，由抽象层自动适配。

### 0.2 API 版本严格遵循
- **开发时必须完全严格按照目标 API 版本使用**，不得使用已弃用（`@Deprecated`）或非公开（内部 `net.minecraft.server.*`）的 API。
- 以 **Paper 26.2 API** 为基准版本进行编译与开发，所有 API 调用以该版本的 JavaDoc 为准（`https://jd.papermc.io/paper/26.2/`）。
- 版本升级时必须同步核对 API 变更日志，对受影响调用进行回归测试；新增依赖需在 `pom.xml` 中显式锁定版本号，避免传递依赖漂移。
- 不得通过反射或字节码注入访问 NMS（net.minecraft.server）私有字段，优先使用 Paper 提供的官方 API；确需底层操作时使用官方提供的 Adventure API、Component API 或 Paper 重映射工具。

### 0.3 参考API库
开发过程中可参考以下权威 API 文档与开源库：

| 库 / 文档 | 用途 | 参考地址 |
|-----------|------|----------|
| **Paper 26.2 API** | 核心 Bukkit/Paper API 基准 | `https://jd.papermc.io/paper/26.2/` |
| **FoliaLib** | Paper/Folia 统一调度与工具抽象 | `https://github.com/handyplus/FoliaLib` |
| **CommandAPI** | 声明式命令注册与参数解析（替代 Brigadier 模板代码） | `https://javadocs.commandapi.dev/` |
| **Adventure API** | 现代 Component 文本组件（取代旧版 ChatColor） | `https://docs.advntr.dev/` |
| **Configurate** | 类型安全的 HOCON/YAML 配置序列化 | `https://configurate.aoeu.xyz/` |
| **Caffeine** | 高性能本地缓存（AI 对话模板、交易价格缓存） | `https://github.com/ben-manes/caffeine` |
| **HikariCP** | 数据库连接池（MySQL/MariaDB 模式） | `https://github.com/brettwooldridge/HikariCP` |

> 选用第三方库时，必须确认其兼容 Folia 的线程模型，否则需自行封装调度适配层。

## 核心功能概述
该插件通过集成大语言模型API，赋予Minecraft村民智能AI行为系统，实现真实的社会模拟与交互体验。

---

## 一、AI集成系统

### 1.1 大模型接口支持
插件内置多个主流大模型接口，支持灵活配置和切换：

- **OpenAI** 
- **Anthropic Claude** 
- **DeepSeek** 
- **智谱GLM** 
- **Moonshot Kimi**
- **第三方兼容接口** (支持OpenAI/Anthropic与格式的自定义端点)

### 1.2 接口配置要求
```yaml
ai:
  provider: "openai"  # 可选: openai, claude, deepseek, glm, kimi, custom
  api-endpoint: "https://api.openai.com/v1"  # 自定义端点
  model: "gpt-3.5-turbo"
  api-key: "your-api-key"
  timeout: 90
  temperature: 0.7
  retry-attempts: 3
```

### 1.3 异步调用架构
所有 AI 请求必须异步执行，并严格区分"纯计算线程"与"游戏世界线程"两个边界：

- **网络/计算层（异步线程）**：使用 `CompletableFuture` 或 Folia 的 `AsyncScheduler`（`Bukkit.getAsyncScheduler()`）执行 HTTP 调用、JSON 解析、上下文组装等无副作用的纯计算任务。
- **世界交互层（区域线程）**：任何对实体、方块、物品、位置的写操作，必须通过调度抽象层回调到该实体/区块所属的区域线程；在 Paper 上回退为主线程。
- **请求队列与限流**：实现基于令牌桶的全局并发限制（如 `ai.max-concurrent-requests`），同一村民的请求串行化，避免短时间大量并发触发限流或 429。
- **超时与重试**：每次请求配置独立超时（默认 90s）与指数退避重试（最多 3 次，初始 1s，上限 8s）；重试次数耗尽后降级到基础行为模式（见 4.5）。
- **结果缓存**：使用 Caffeine 缓存高频对话模板、决策结果与交易价格，键值含村民 UUID + 场景类型，TTL 默认 300s，降低 API 调用频次与成本。
- **上下文裁剪**：发送前对 AI 记忆进行摘要与裁剪（保留最近 N 条 + 关键事实），控制 `max-tokens`，避免 token 超限与上下文膨胀。
- **线程安全**：跨线程共享的可变状态必须使用 `ConcurrentHashMap`、`AtomicReference` 或显式锁；AI 决策结果通过不可变对象（`record`）在线程间传递。

---

## 二、村民职业系统

### 2.1 职业类型与属性
村民生成时随机分配职业，每个职业具有独特的属性、装备和行为模式：

| 职业 | 生命值 | 攻击力 | 防御 | 主要武器 | 特殊能力 |
|------|--------|--------|------|----------|----------|
| **国王 (King)** | 100 | 15 | 高 | 黄金剑 | 发布命令、统筹建设 |
| **骑士 (Knight)** | 80 | 20 | 高 | 铁剑+盾牌 | 巡逻、保护村民 |
| **士兵 (Soldier)** | 60 | 15 | 中 | 铁剑 | 战斗、防御 |
| **弓箭手 (Archer)** | 50 | 12 | 低 | 弓箭 | 远程攻击 |
| **屠夫 (Butcher)** | 50 | 10 | 中 | 斧头 | 肉类交易、动物处理 |
| **厨师 (Chef)** | 40 | 5 | 低 | 无 | 食物制作与交易 |
| **农民 (Farmer)** | 40 | 5 | 低 | 锄头 | 种植、收获作物 |
| **矿工 (Miner)** | 50 | 8 | 中 | 镐子 | 采矿、资源收集 |
| **建筑师 (Builder)** | 45 | 5 | 中 | 无 | 建造建筑 |
| **商人 (Merchant)** | 40 | 5 | 低 | 无 | 高级交易 |
| **普通人 (Civilian)** | 40 | 5 | 低 | 无 | 基础生活行为 |

### 2.2 职业分配机制
职业分配采用"村庄上下文 + 加权随机 + 角色约束"的多层策略，确保村庄生态合理且可玩性平衡：

```java
// 生成时按权重随机分配
职业分布权重:
- 普通人: 40%
- 农民: 20%
- 商人/厨师/屠夫: 各5%
- 士兵/骑士: 各10%
- 弓箭手/矿工/建筑师: 各3%
- 国王: 每个村庄仅1位（首次生成或特殊触发）
```

**分配流程**：
1. **村庄识别**：村民生成时先判定所属村庄（依据现有村庄的半径覆盖或新建村庄）。
2. **国王约束**：若该村庄尚无国王且满足生成条件（人口 ≥ 阈值或玩家指定），优先尝试生成国王；已有国王则跳过该职业。
3. **加权随机**：在剩余可分配职业池中按上表权重进行加权随机抽取。
4. **需求平衡**：当某职业数量超出村庄配额上限（如战斗职业过多），自动降权或转投相近职业，避免村庄职业结构失衡。
5. **可继承性**：村民繁殖的后代有概率继承父/母职业（基础继承率 30%，可配置），未继承则落入普通人池。
6. **玩家干预**：管理员可通过 `/bv profession <村民> <职业>` 强制指派，指派后锁定不再随机变更。

**触发时机**：
- 村民首次自然生成、繁殖生成、`CreatureSpawnEvent` 捕获。
- 转职冷却（同一村民非管理员指派时，职业变更需间隔 ≥ 1 游戏日）。

### 2.3 装备与外观
- 职业对应的装备在村民生成时自动装备
- 使用自定义模型数据或皮革染色区分职业外观
- 装备具有耐久度，可通过交易修复

**外观分层与实现**：
- **装备槽位**：主手（武器/工具）、头盔、胸甲、护腿、靴子，通过 `EntityEquipment` 在区域线程上写入；Folia 下必须经实体调度器（`entity.getScheduler()`）执行。
- **职业标识**：采用三重视觉区分——皮革染色（基础色）、自定义模型数据（`ItemMeta.setCustomModelData`，配合资源包）、头顶粒子/标题（如国王金色光环、骑士盾牌粒子）。
- **外观与属性解耦**：装备外观（显示层）与战斗属性（数值层）分离存储，避免耐久度归零后属性异常丢失。

**耐久与修复机制**：
- 装备耐久随使用/战斗缓慢消耗（受 `professions.<职业>.durability-loss-rate` 控制）。
- 屠夫/铁匠类村民可对其它村民装备进行修复（消耗原材料），玩家亦可付费修复。
- 耐久归零不破坏装备，而是降级为"破损"状态（属性减半），修复后恢复，避免村民意外裸装。

**动态外观**：
- 战斗状态切换外观（如骑士进入战斗自动举盾）。
- 夜间/雨天战斗职业主动切换为防御姿态外观。
- 装备更新统一通过 `EquipmentUpdateEvent` 内部事件总线分发，便于扩展第三方资源包外观。

---

## 三、智能行为系统

### 3.1 趋利避害AI
村民能够通过AI模型判断并执行以下行为：

**威胁识别**:
- 识别敌对生物（僵尸、骷髅、掠夺者等）
- 识别攻击村民的玩家（记录为"非法玩家"）
- 识别环境危险（岩浆、悬崖、火焰）

**应对策略**:
- 低战斗力职业：逃跑并寻求保护
- 战斗职业：主动反击，保护其他村民
- 呼叫附近的骑士/士兵支援
- 夜间自动返回安全区域

**决策分层（混合架构）**：
为兼顾响应速度与 AI 成本，行为决策分为三层：
- **反射层（无 AI，毫秒级）**：基于规则引擎处理紧急场景（被攻击瞬间的闪避、逃离岩浆、扑灭身上的火），不调用大模型，保证生存反应零延迟。
- **战术层（轻量 AI，秒级）**：周期性（`ai-update-interval`）调用大模型处理中频决策（是否迎战、是否呼叫支援、选择巡逻路线），输入为压缩后的战场态势快照。
- **战略层（深度 AI，分钟级）**：国王/队长级别调用，制定村庄发展规划、资源调度与任务下发，频率最低以控制 token 成本。

**行为状态机**：每个村民维护有限状态机（FSM：空闲 / 工作 / 战斗 / 逃跑 / 休息 / 交易），状态切换由上述三层决策驱动，并触发对应的调度器任务（战斗走高频 tick，空闲走低频 tick）。

**记忆与性格**：
- 每个村民拥有独立的性格参数（勇敢/谨慎/贪婪等，影响 AI prompt 偏置）。
- 短期记忆（最近事件）与长期记忆（关键人物关系、恩怨）分层存储，长期记忆定期摘要落库。

### 3.2 方块交互能力
```java
村民可以执行的方块操作:
- 破坏方块: 收集资源（木头、石头、农作物）
- 放置方块: 建造建筑、修复损坏、种植作物
- 使用工作台: 合成物品（厨师做食物、建筑师做建材）
- 使用容器: 存储物品到箱子、从箱子取物品
```

**交互能力分级**：
- 每个职业拥有独立的"方块操作白名单"（如农民可耕地/种植/收获，矿工可挖掘矿石/放置火把），由 `professions.yml` 配置，超出白名单的操作被拒绝。
- 操作分为读取（判定方块类型）、破坏、放置、交互（打开/合成）四类，分别受独立冷却与权限校验。

**限制与权限**:
- 不可破坏玩家标记的保护区域方块
- 不可破坏重要建筑（配置文件定义）
- 方块操作需消耗村民的"行动点数"，避免过度刷方块

**线程与性能保障**：
- **Folia 区域调度**：方块操作的目标坐标已知时，通过 `RegionScheduler` 的 `execute(...)` / `runDelayed(...)` 调度到该坐标所在区域线程；不可在异步线程直接调用 `world.getBlockAt()` 的写方法。
- **批量合并**：连续的同类操作（如建筑师连续放置）合并为单次区域线程提交，减少跨线程调度开销。
- **异步预计算 + 主区域提交**：寻路、地形分析、建筑蓝图解析在异步线程完成，仅最终 `setType` / `placeBlock` 提交至区域线程。
- **回滚机制**：所有方块变更记录到可重放的变更日志，异常或误操作时可按村庄/时间段批量回滚，保护玩家建筑。

### 3.3 组织化行为

**国王系统**:
- 每个村庄选举或生成1位国王
- 国王通过AI接口制定村庄发展计划
- 发布建造任务（城墙、房屋、农田）
- 分配村民职责和巡逻路线

**城邦建设**:
```yaml
建造任务类型:
- 城墙: 骑士和士兵建议后，建筑师+普通人执行
- 农田: 国王规划，农民执行种植和维护
- 房屋: 建筑师设计并带领村民建造
- 道路: 铺设村庄内外的道路网络
```

**集体活动**:
- 定期举行市集（商人集中交易）
- 防御演习（士兵集结训练）
- 收获庆典（农民展示收成）
- 建筑竞赛（建筑师比拼技艺）

### 3.4 自主交易系统
- 村民之间可以自主交易（农民卖食物给厨师）
- 根据供需关系动态调整交易价格
- AI判断交易是否公平并决定接受或拒绝
- 玩家交易界面保留，但价格由AI动态生成

---

## 四、技术实现要求

### 4.1 异步架构 (Async Mode)
**强制要求**:
- 所有AI调用必须异步执行
- 所有路径寻找使用异步算法
- 方块操作在主线程外预计算，仅在主线程执行最终操作
- 使用线程池管理，避免无限创建线程

**Folia 区域线程调度模型**：
Folia 将世界切分为多个"区域"（Region），每个区域由独立线程 tick，跨区域/跨线程操作必须显式调度。插件通过统一调度抽象层封装以下三类调度：
- **实体调度（`EntityScheduler`）**：与某村民绑定的操作（移动、装备、攻击）使用 `entity.getScheduler().execute(plugin, task, null, retired)`，确保操作在该实体所在区域线程执行；实体被卸载/移除时自动回调 `retired`。
- **区域调度（`RegionScheduler`）**：已知坐标但无实体绑定的操作（方块读写、位置探查）使用 `Bukkit.getRegionScheduler().execute(plugin, location, task)`，调度到该坐标所属区域线程。
- **全局区域调度（`GlobalRegionScheduler`）**：无具体坐标的全局任务（世界级别统计、定时保存）使用 `Bukkit.getGlobalRegionScheduler().runAtFixedRate(...)`。
- **异步调度（`AsyncScheduler`）**：纯计算/网络任务使用 `Bukkit.getAsyncScheduler().runNow(...)`，运行于 ForkJoinPool，禁止在其中触碰任何游戏世界对象。

```java
// 示例架构（Folia/Paper 统一抽象）
CompletableFuture.supplyAsync(() -> {
    // 纯计算线程：AI API 调用，无游戏对象访问
    return aiService.getDecision(villagerContext);
}).thenAcceptAsync(decision -> {
    // 通过调度抽象层将执行回调到实体所在区域线程
    scheduler.entity(villager, () -> {
        villager.executeDecision(decision);  // 安全的世界交互
    });
}, asyncExecutor);
```

> 抽象层在运行期探测 `Bukkit.getServer()` 是否为 Folia（`Class.forName("io.papermc.paper.threadedregions.RegionizedServer")`），自动选择对应实现，业务代码只依赖抽象接口。

### 4.2 内存管理
- 实现村民数据缓存，定期清理不活跃村民的内存占用
- AI对话历史限制最大条目数（例如50条）
- 使用弱引用存储非关键数据
- 区块卸载时暂停该区块村民的AI任务
- JVM参数建议：`-Xmx4G -Xms2G -XX:+UseG1GC`

### 4.3 原版兼容性
**避免的BUG**:
- 不干扰原版村民繁殖机制
- 不破坏铁傀儡生成逻辑
- 不影响袭击事件的触发和进行
- 保持村民与工作站点的绑定关系
- 不修改原版交易表结构（仅动态调整价格）

### 4.4 性能优化
```yaml
性能配置建议:
- ai-update-interval: 5  # AI思考间隔（秒）
- max-active-ai-villagers: 50  # 同时激活AI的村民数量
- pathfinding-range: 32  # 寻路最大范围
- block-operation-cooldown: 10  # 方块操作冷却（秒）
- trade-calculation-cache: 300  # 交易价格缓存时间（秒）
```

### 4.5 稳定性保障
- 全局异常捕获，AI错误不导致服务器崩溃
- API调用失败时降级到基础行为模式
- 定期保存村民状态到数据库
- 提供 `/bettervillagers debug` 命令查看运行状态
- 集成日志系统记录所有AI决策和异常

**多级容错与降级策略**：
- **熔断器（Circuit Breaker）**：当 API 错误率超过阈值（如 1 分钟内失败 ≥ 50%），自动熔断该 provider 一段时间（默认 60s），期间全部村民降级为规则驱动的反射层行为，避免雪崩。
- **三级降级链**：大模型不可用 → 切换备用 provider → 切换缓存决策 → 纯规则引擎，确保任何情况下村民行为不中断、服务器不崩溃。
- **线程边界守护**：所有异步任务包裹 try-catch，捕获异常后仅记录日志并降级，禁止异常跨线程传播到区域线程导致 tick 卡顿或崩溃。
- **Folia 一致性校验**：关键写操作前校验当前线程是否为预期区域线程（断言式检查，`debug-mode` 下抛出明确异常），防止开发期引入隐式线程违规。

**数据安全**：
- 采用 WAL（Write-Ahead Logging）式定时保存（默认 5 分钟）+ 关键事件即时落盘，服务器异常重启后可恢复到最后一致状态。
- 数据库写入失败时写入本地降级文件，启动时自动补录，避免数据丢失。
- 配置/数据文件修改支持原子替换（写临时文件后 rename），防止写入中途崩溃导致文件损坏。

**可观测性**：
- `/bettervillagers debug` 输出：在线村民数、活跃 AI 任务数、各 provider 延迟/错误率、各区域线程负载、熔断状态。
- 指标埋点（可选对接 Micetrics）暴露 QPS、P95 延迟、token 消耗，便于运维监控。
- 关键决策与异常分级日志（INFO/WARN/ERROR），可按村庄/村民过滤，支持日志轮转避免磁盘膨胀。

---

## 五、生电模式 (Redstone Mode)

### 5.1 模式说明
为了不影响技术玩家的红石机器和村民交易所，提供"生电模式"配置：

```yaml
redstone-mode:
  enabled: true
  protected-regions:
    - world: "world"
      x1: 100
      y1: 60
      z1: 100
      x2: 200
      y2: 80
      z2: 200
      owner: "PlayerName"  # 有权限的玩家
```

### 5.2 模式行为
**生电模式区域内**:
- 村民AI完全关闭，恢复纯原版逻辑
- 不进行AI决策和行为判断
- 交易价格和机制与原版完全一致
- 不进行任何方块交互
- 不参与组织化活动
- 繁殖、工作、交易均为原版行为

**生电模式区域外**:
- 正常启用所有AI功能

### 5.3 权限管理
```yaml
权限节点:
- bettervillagers.redstone.create  # 创建生电保护区
- bettervillagers.redstone.modify  # 修改保护区
- bettervillagers.redstone.delete  # 删除保护区
- bettervillagers.admin  # 完全管理权限
```

### 5.4 可视化界面
- 使用 `/bv region create` 通过WorldEdit选区创建保护区
- 使用粒子效果显示保护区边界
- 提供GUI界面管理多个保护区

---

## 六、配置文件结构

### 6.1 主配置 (config.yml)
```yaml
# 插件主配置
plugin:
  language: "zh_CN"
  debug-mode: false
  auto-save-interval: 300  # 秒

# AI配置
ai:
  provider: "openai"
  api-key: ""
  api-endpoint: ""
  model: "gpt-3.5-turbo"
  timeout: 30
  max-tokens: 500
  temperature: 0.7

# 性能配置
performance:
  async-threads: 4
  max-active-villagers: 50
  ai-update-interval: 5
  pathfinding-range: 32

# 功能开关
features:
  ai-behavior: true
  block-interaction: true
  autonomous-building: true
  self-defense: true
  auto-trading: true

# 生电模式
redstone-mode:
  enabled: true
  default-protected: false

# 职业配置
professions:
  king:
    enabled: true
    max-per-village: 1
  knight:
    spawn-weight: 10
    equipment:
      - "IRON_SWORD"
      - "SHIELD"
  # ... 其他职业
```

### 6.2 语言文件 (lang/zh_CN.yml)
```yaml
messages:
  prefix: "&7[&6BetterVillagers&7] "
  villager-attacked: "&c村民 {name} 正在遭受攻击！"
  building-started: "&a村民开始建造 {structure}"
  king-command: "&6国王 {name}: {command}"
  # ... 更多消息
```

### 6.3 职业数据 (professions.yml)
详细定义每个职业的属性、装备、行为权重等。

```yaml
professions:
  # ============ 国王 ============
  king:
    enabled: true
    max-per-village: 1
    spawn-weight: 0            # 不参与常规加权随机
    inherit-rate: 0.0
    stats:
      health: 100
      attack: 15
      defense: HIGH
    equipment:
      main-hand: { material: GOLDEN_SWORD, custom-model-data: 1001 }
      helmet:    { material: GOLDEN_HELMET, custom-model-data: 1002 }
      chestplate:{ material: GOLDEN_CHESTPLATE, custom-model-data: 1003 }
    durability-loss-rate: 0.5  # 每次使用耐久损耗倍率
    behavior-weights:          # AI 决策权重
      command: 0.6
      build: 0.2
      trade: 0.2
    personality: { bravery: 0.8, greed: 0.5 }
    block-whitelist: []        # 国王不直接操作方块

  # ============ 骑士 ============
  knight:
    enabled: true
    spawn-weight: 10
    inherit-rate: 0.3
    stats:
      health: 80
      attack: 20
      defense: HIGH
    equipment:
      main-hand: { material: IRON_SWORD, custom-model-data: 2001 }
      off-hand:  { material: SHIELD, custom-model-data: 2002 }
      chestplate:{ material: IRON_CHESTPLATE }
    durability-loss-rate: 1.0
    behavior-weights:
      patrol: 0.4
      combat: 0.5
      rest: 0.1
    personality: { bravery: 0.9, greed: 0.2 }
    block-whitelist: []

  # ============ 农民 ============
  farmer:
    enabled: true
    spawn-weight: 20
    inherit-rate: 0.3
    stats:
      health: 40
      attack: 5
      defense: LOW
    equipment:
      main-hand: { material: WOODEN_HOE, custom-model-data: 3001 }
    durability-loss-rate: 0.8
    behavior-weights:
      farm: 0.7
      trade: 0.2
      rest: 0.1
    personality: { bravery: 0.2, greed: 0.4 }
    block-whitelist:
      - FARMLAND          # 耕地
      - WHEAT             # 收获
      - CARROTS
      - POTATOES
      - BEETROOTS

  # ============ 矿工 ============
  miner:
    enabled: true
    spawn-weight: 3
    inherit-rate: 0.3
    stats:
      health: 50
      attack: 8
      defense: MEDIUM
    equipment:
      main-hand: { material: IRON_PICKAXE, custom-model-data: 4001 }
      helmet:    { material: IRON_HELMET }
    durability-loss-rate: 1.2
    behavior-weights:
      mine: 0.7
      rest: 0.3
    personality: { bravery: 0.5, greed: 0.6 }
    block-whitelist:
      - STONE
      - COAL_ORE
      - IRON_ORE
      - GOLD_ORE
      - DIAMOND_ORE
      - ".*_ORE"          # 支持正则匹配

  # ... 厨师(chef)、屠夫(butcher)、商人(merchant)、建筑师(builder)、
  #     士兵(soldier)、弓箭手(archer)、普通人(civilian) 等同理配置
```

---

## 七、命令系统

```
/bettervillagers (别名: /bv)
  ├─ reload - 重载配置
  ├─ debug - 查看调试信息
  ├─ profession <村民> <职业> - 设置村民职业
  ├─ region
  │   ├─ create <名称> - 创建生电保护区
  │   ├─ delete <名称> - 删除保护区
  │   ├─ list - 列出所有保护区
  │   └─ info <名称> - 查看保护区详情
  ├─ village
  │   ├─ info - 查看村庄信息
  │   ├─ king - 查看国王信息
  │   └─ stats - 查看村庄统计
  └─ ai
      ├─ toggle <村民> - 切换村民AI
      ├─ reset <村民> - 重置村民AI记忆
      └─ test <村民> <prompt> - 测试AI响应
```

---

## 八、数据存储

### 8.1 数据库选择
- 优先使用 **SQLite**（轻量级，无需外部依赖）
- 可选 **MySQL/MariaDB**（多服务器同步）

### 8.2 数据表结构
```sql
-- 村民数据表
CREATE TABLE villagers (
    uuid VARCHAR(36) PRIMARY KEY,
    name VARCHAR(64),
    profession VARCHAR(32),
    health DOUBLE,
    attack DOUBLE,
    defense DOUBLE,
    location_world VARCHAR(64),
    location_x DOUBLE,
    location_y DOUBLE,
    location_z DOUBLE,
    village_id INT,
    ai_memory TEXT,  -- JSON格式存储AI对话历史
    created_at TIMESTAMP,
    updated_at TIMESTAMP
);

-- 村庄数据表
CREATE TABLE villages (
    id INT AUTO_INCREMENT PRIMARY KEY,
    world VARCHAR(64),
    center_x INT,
    center_y INT,
    center_z INT,
    radius INT,
    king_uuid VARCHAR(36),
    population INT,
    created_at TIMESTAMP
);

-- 保护区数据表
CREATE TABLE protected_regions (
    id INT AUTO_INCREMENT PRIMARY KEY,
    name VARCHAR(64) UNIQUE,
    world VARCHAR(64),
    min_x INT,
    min_y INT,
    min_z INT,
    max_x INT,
    max_y INT,
    max_z INT,
    owner VARCHAR(36),
    created_at TIMESTAMP
);
```

---

## 九、测试要求

### 9.1 单元测试
- AI接口调用测试
- 职业系统测试
- 异步任务测试
- 数据库CRUD测试

### 9.2 集成测试
- 村民生成与职业分配
- 战斗行为测试
- 建造系统测试
- 生电模式测试

### 9.3 性能测试
- 100个村民同时激活AI的性能表现
- 内存泄漏检测
- 长时间运行稳定性测试

### 9.4 兼容性测试
- Spigot 1.20+
- Paper 1.20+
- Purpur 1.20+
- 与常见插件兼容性（WorldEdit、WorldGuard、Vault等）

---

## 十、开发里程碑

### Phase 1: 基础框架 (2周)
- Maven项目搭建
- AI接口集成与测试
- 基础职业系统
- 配置文件系统
- **调度抽象层落地**：基于 FoliaLib 实现 Paper/Folia 统一调度器，运行期自动探测平台，封装实体/区域/全局/异步四类调度接口，并编写线程边界单元测试。
- **API 版本对齐**：锁定 Paper 26.2 API 为编译基准，扫描并清除所有 `@Deprecated` 调用与 NMS 反射，建立依赖版本台账。

### Phase 2: 智能行为 (3周)
- 趋利避害AI
- 战斗系统
- 方块交互
- 路径寻找
- **三层决策混合架构**：实现反射层规则引擎、战术层 AI 调用、战略层规划调度，验证响应延迟与 token 成本。
- **方块交互引擎**：按职业白名单实现读取/破坏/放置/交互四类操作，全部经区域调度提交，附带变更日志与回滚能力。
- **异步寻路**：基于 A*/JPS 的异步寻路实现，结果回传区域线程驱动村民移动，避免主线程卡顿。

### Phase 3: 社会系统 (3周)
- 国王系统
- 组织化建造
- 自主交易
- 集体活动
- **村庄与角色约束**：实现村庄识别、国王唯一性、职业分配加权随机与配额平衡。
- **装备与外观系统**：装备槽位写入、自定义模型/染色三重视觉区分、耐久与破损降级机制。
- **经济循环**：自主交易供需定价、AI 公平性判断、动态价格缓存。

### Phase 4: 优化与生电模式 (2周)
- 性能优化
- 内存管理
- 生电模式实现
- Bug修复
- **Folia 压力测试**：在多区域高负载下验证调度正确性与线程一致性，消除隐式主线程假设。
- **熔断与降级链**：实现 Circuit Breaker、多 provider 容灾与规则降级回退。
- **生电模式**：保护区内完全关闭 AI，粒子可视化边界与 WorldEdit 选区集成。

### Phase 5: 测试与发布 (1周)
- 完整测试
- 文档编写
- 发布准备
- **三平台兼容矩阵**：Spigot / Paper / Folia 三端回归测试，WorldEdit、WorldGuard、Vault 等常见插件交叉验证。
- **可观测性收尾**：完善 `/bv debug` 指标输出与日志分级，发布性能基线报告。

---